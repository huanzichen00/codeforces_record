#include <bits/stdc++.h>
using namespace std;

using ll = long long;

// 取子串
string sub(const string &s, int l, int r) {
  return s.substr(l, r - l); // 左闭右开
}

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);


}
