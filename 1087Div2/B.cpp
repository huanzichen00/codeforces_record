#include <bits/stdc++.h>
using namespace std;

using ll = long long;

const int MAXN = 5005;
int arr[MAXN];

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int tt;
  while (tt--) {
    int n;
    cin >> n;
    for (int i = 0; i < n; i++) {
      cin >> arr[i];
    }
    
  }
  return 0;
}
